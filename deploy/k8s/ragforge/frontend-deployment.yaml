apiVersion: apps/v1
kind: Deployment
metadata:
  name: ragforge-frontend
  namespace: ragforge
  labels:
    app: ragforge-frontend
spec:
  strategy:
    type: Recreate
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: ragforge-frontend
  template:
    metadata:
      labels:
        app: ragforge-frontend
    spec:
      containers:
        - name: frontend
          image: docker.io/ragforge/frontend:0.0.1
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
          ports:
            - name: http
              containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 192Mi
