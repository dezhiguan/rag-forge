<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

  <!-- 默认彩色控制台；需要 JSON（Filebeat）时追加 profile：json-logs -->
  <springProfile name="!json-logs">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <pattern>%clr(%d{HH:mm:ss.SSS}){faint} %highlight(%-5level) %clr([traceId=%X{traceId}]){magenta} %clr(%logger{36}){cyan} %clr(-){faint} %msg%n</pattern>
        <charset>UTF-8</charset>
      </encoder>
    </appender>

    <!-- RAG 检索链路：绿色标签 + 加粗，便于在控制台快速定位 -->
    <appender name="RAG_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <pattern>%clr(%d{HH:mm:ss.SSS}){faint} %highlight(%-5level) %clr([RAG]){green,bold} %clr([traceId=%X{traceId}]){magenta} %clr(%logger{36}){green} %clr(-){faint} %msg%n</pattern>
        <charset>UTF-8</charset>
      </encoder>
    </appender>

    <logger name="com.ragforge.search" level="INFO" additivity="false">
      <appender-ref ref="RAG_CONSOLE"/>
    </logger>

    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>

  <springProfile name="json-logs">
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="net.logstash.logback.encoder.LogstashEncoder">
        <includeMdcKeyName>traceId</includeMdcKeyName>
        <fieldNames>
          <timestamp>@timestamp</timestamp>
        </fieldNames>
      </encoder>
    </appender>
    <root level="INFO">
      <appender-ref ref="CONSOLE"/>
    </root>
  </springProfile>
</configuration>
